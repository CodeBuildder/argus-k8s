# Falco custom rules — placeholder
# TODO: populate in Module 2
#
# Add project-specific detection rules here.
# Do not modify the default Falco ruleset — override or extend here.
#
# Example rule structure:
# - rule: Argus - Unexpected outbound connection
#   desc: Detects outbound connections from pods not expected to make them
#   condition: outbound and not proc.name in (allowed_processes)
#   output: "Unexpected outbound connection (proc=%proc.name pod=%k8s.pod.name ns=%k8s.ns.name)"
#   priority: WARNING
#   tags: [network, argus]
