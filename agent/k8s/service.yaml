apiVersion: v1
kind: Service
metadata:
  name: argus-agent
  namespace: argus-system
  labels:
    app: argus-agent
spec:
  selector:
    app: argus-agent
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
